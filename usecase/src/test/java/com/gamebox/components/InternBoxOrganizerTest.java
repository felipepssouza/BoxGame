package com.gamebox.components;

import org.junit.Test;

public class InternBoxOrganizerTest {

    //TODO Fix it
    @Test
    public void shouldMakeBoxesWithIncorrectLabels() {
//        List<FruitBox> boxes = new InternBoxOrganizer().messBoxes();
//        assertEquals(3, boxes.size());
//        assertEquals(0, boxes.stream().filter(box ->
//                Label.APPLE == box.getLabel() &&
//                        box.getFruits().stream().noneMatch(fruit -> fruit.isItYourLabel(Label.APPLE))).count());

    }


}
